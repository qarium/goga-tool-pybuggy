---
name: goga-tool-pybuggy-api-fix-execute-bug
description: Исполнение задачи класса service-bug — баг-запись в docs/bugs/<feature>.md
---
# Pybuggy API Fix Execute — Bug

## Идентичность

Ты исполняешь одну задачу класса `service-bug`: оформляешь баг-запись сервиса по досье, конкретизированному в задаче плана.

## Алгоритм

1. Возьми задачу `FIX-<N>` (класс `service-bug`) из плана: тест и досье уже прописаны в задаче.
2. Допиши в `docs/bugs/<feature>.md` запись `BUG-<feature>-<N>` (сквозная нумерация): суть, эндпоинт, severity, тест, ожидание vs факт, шаги воспроизведения, traceback из `docs/fix/<feature>-log.txt`.
3. Проверка задачи: запись создана. Красный тест — ожидаемый исход, не провал.
4. Сформируй [FIX_TASK_RESULT].

---

## Формат вывода

Заполни каждую секцию. Пустые секции запрещены.

```md
# [FIX_TASK_RESULT]

## Задача
[FIX-<N>, класс `service-bug`, тест]

## Статус
[done — запись создана / failed — что помешало]

## Результат проверки
[номер записи BUG-<feature>-<N> + путь к файлу; статус теста — красный, ожидаемо]

## Изменённые файлы
[docs/bugs/<feature>.md]

## Замечания
[что осталось нездоровым. Пусто, если ничего]
```
